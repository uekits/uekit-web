# 内部包说明

`internal` 用于放置尚未形成稳定公开 API 的运行时契约、工具、共享配置和实验性能力。

规则：

- 这里的内容默认不是 UEKit Web 公共 API。
- 只有被多个真实公开能力验证后，才能提升为稳定模块或 Registry 条目。
- 业务项目不得直接依赖 `internal/*`。
- 内部包也必须通过类型、Lint 和测试检查。
- 提升为公共能力时必须补齐中文文档、兼容策略和 Changeset。
