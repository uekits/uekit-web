{
    "name": "@uekits/web-playground",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "license": "MIT",
    "scripts": {
        "dev": "vite",
        "build": "vue-tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json && vite build",
        "typecheck": "vue-tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json"
    },
    "dependencies": {
        "@amap/amap-jsapi-loader": "catalog:",
        "@lucide/vue": "catalog:",
        "echarts": "catalog:",
        "element-plus": "catalog:",
        "vue": "catalog:"
    },
    "devDependencies": {
        "@tailwindcss/vite": "catalog:",
        "tailwindcss": "catalog:"
    }
}
